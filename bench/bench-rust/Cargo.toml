[package]
name = "bench-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
tokio-postgres = "0.7"
rust_decimal = { version = "1", features = ["db-tokio-postgres"] }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
